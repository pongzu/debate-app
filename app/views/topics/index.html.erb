<div class= "indexArea">
<div class="main posts-index">
    <% @topics.each do |topic| %>
      <div class="posts-index-item">
         <div class="topic-area"> 
         <p class="user"><%= link_to(topic.user.name,"/users/#{topic.user.id}",class:"link-delite") %></p>
           <%= link_to(topic.content,"/posts/#{topic.id}",class: "content") %>
         </div>
          <div class="join-box">
　          <%= link_to("Join this discussion🔥","/topics/#{topic.id}",class: "join-for")%>
　        </div>
      </div>
    <% end %>
</div>
</div>
